[build]
  command = "hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.145.0"  # Use a recent stable version

# Ensure submodules are initialized
[build.processing]
  skip_processing = false
